import React from "react";
import { Typography, Container } from "@mui/material";

export default function Analyses() {
    return (
        <Container>
            <Typography variant="h4" component="h1" style={{ marginTop: "2rem", textAlign: "center" }}>Analyses</Typography>
            <Typography variant="h6" component="h1" style={{ marginTop: "2rem" }}>Quantitative Analysis</Typography>
        </Container>
    )
}